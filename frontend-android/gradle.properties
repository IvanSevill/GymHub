# Gradle memory settings
# The currently configured max heap space was 512MB, increasing to 2GB
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# AndroidX - required for Compose and all modern Android dependencies
android.useAndroidX=true

# Enable parallel builds for faster compilation
org.gradle.parallel=true

# Optimization: caching
org.gradle.caching=true

# Enable parallel builds for faster compilation
org.gradle.parallel=true

# Optimization: caching
org.gradle.caching=true

# Disable performance logging (unnecessary for development)
# org.gradle.daemon.performance.disable-logging=true
